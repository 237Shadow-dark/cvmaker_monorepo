{
  "name": "cv-maker-app",
  "version": "1.0.0",
  "description": "A web application to create and manage CVs",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "turbo run --recursive build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.8",
    "typescript": "^5.2.2"
  }
}
